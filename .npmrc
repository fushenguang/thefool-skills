# 刻意不在这里写 only-built-dependencies。
# .npmrc 只能存字符串，pnpm 会把 `only-built-dependencies = a,b,c` 映射成一个
# **字符串**，而 pnpm/action-setup 的 self-installer 对它调用 .sort()，直接崩：
#   ERROR onlyBuiltDependencies?.sort is not a function
# 该设置的真源是 package.json 的 `pnpm.onlyBuiltDependencies`（数组），只留那一份。
